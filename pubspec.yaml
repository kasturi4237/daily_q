name: daily_quotes_app
description: A simple daily quotes Flutter app

publish_to: 'none' # Remove this line if you want to publish

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  share_plus: ^7.2.1
  flutter_lints: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
